/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    unoptimized: true, // Set this to true to enable Image Optimization
  },
};

module.exports = nextConfig;
